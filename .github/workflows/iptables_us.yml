name: PUSH
on:
  push:
    paths:
      - "iptables/dmit-us/release.md"
env:
  XRAY_DIR: iptables
  DIR_PATH: dmit-us
  IPTABLES_FILE: iptables.conf

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout master
        uses: actions/checkout@v3
        with:
          ref: main

      - name: Get current date
        id: date
        run: echo "::set-output name=today::$(date +'%Y%m%d%H%M')"
        shell: bash
      - name: copy file via ssh key
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST_US }}
          username: ${{ secrets.VPS_USERNAME }}
          port: ${{ secrets.VPS_PORT }}
          key: ${{ secrets.VPS_KEY_US }}
          source: "./${{ env.XRAY_DIR }}/${{ env.DIR_PATH }}/${{ env.IPTABLES_FILE }}"
          target: "/root"
      - name: restore iptables from file
        uses: appleboy/ssh-action@master
        with:
          overwrite: true
          host: ${{ secrets.VPS_HOST_US }}
          username: ${{ secrets.VPS_USERNAME }}
          port: ${{ secrets.VPS_PORT }}
          key: ${{ secrets.VPS_KEY_US }}
          script: |
            iptables-restore < /root/${{ env.XRAY_DIR }}/${{ env.DIR_PATH }}/${{ env.IPTABLES_FILE }}
            /usr/sbin/netfilter-persistent save
            echo "${{ steps.date.outputs.today }}" >> it_change
